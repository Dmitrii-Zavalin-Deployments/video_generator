{
  "type": "object",
  "description": "Root schema for the video generation task.",
  "properties": {
    "inputs": {
      "type": "object",
      "description": "Input files and resources required for the video generation process.",
      "properties": {
        "processed_frames_zip_path": {
          "type": "string",
          "description": "Path to the zip archive containing the processed frames to be compiled."
        },
        "output_video_path": {
          "type": "string",
          "description": "Path where the final generated video file will be saved."
        }
      },
      "required": [
        "processed_frames_zip_path",
        "output_video_path"
      ]
    },
    "config": {
      "type": "object",
      "description": "Configuration settings for frame rate",
      "properties": {
        "fps": {
          "type": "number",
          "minimum": 1,
          "description": "Frames per second for the output video."
        }
      },
      "required": [
        "fps"
      ]
    },
    "results": {
      "type": "object",
      "description": "Execution output details including final status and error logs if any.",
      "properties": {
        "status": {
          "type": "string",
          "description": "Current status of the execution (e.g., success, error)."
        },
        "error": {
          "type": "string",
          "description": "Error description if the video generation fails."
        },
        "date_time": {
          "type": "string",
          "description": "Timestamp of when the execution results were compiled in ISO 8601 format."
        }
      },
      "required": [
        "status",
        "error",
        "date_time"
      ]
    }
  },
  "required": [
    "inputs",
    "config",
    "results"
  ]
}